# Laffyhand Configuration
# Copy this file to laffyhand.yml and fill in your settings.

llm:
  # Provider key to use by default (must match a key under `providers`)
  default_provider: "opencode"
  # Multiple LLM provider configurations
  providers:
    opencode:
      # Provider type: "openai" or "deepseek"
      type: "deepseek"
      # API endpoint
      base_url: "https://opencode.ai/zen/go"
      # API key for authentication
      api_key: "sk-..."
      # Available models for this provider
      models:
        - name: "deepseek-v4-flash"
          context_size: 1000000

db:
  # Path to the SQLite database file
  path: "./laffyhand.db"

logging:
  # Directory for log files
  dir: "logs"
  # Log level: DEBUG, INFO, WARNING, ERROR, CRITICAL
  level: "INFO"
  # Number of days to retain log files
  retention_days: 10
  # Whether to also output logs to console
  console: true

agent:
  # Title generation mode: off, on_create, on_compact, auto
  title_mode: "on_compact"
  # Number of recent user turns preserved verbatim during compaction
  compaction_tail_turns: 2
  # Maximum agent loop iterations per turn
  max_steps: 50
  # Maximum concurrent sub-agents
  max_concurrent_subagents: 2

paths:
  # Directories to scan for SKILL.md files
  skills: []
  # Directories to scan for .agent.md files (optional)
  agents: []

memory:
  # Whether the memory system is enabled
  enabled: true
  # Maximum character length of Memory.md
  max_length: 10000
  # Path to the memory file (relative to cwd or absolute)
  path: "./Memory.md"

mcp:
  # MCP server configurations
  servers:
    # example local MCP server:
    # my-local-server:
    #   type: local
    #   command: ["python", "-m", "my_mcp_server"]
    #   env:
    #     MY_VAR: "value"
    #   timeout: 300
    #
    # example remote MCP server:
    # my-remote-server:
    #   type: remote
    #   url: "https://example.com/sse"
    #   transport: sse  # sse or streamable-http
    #   timeout: 300
