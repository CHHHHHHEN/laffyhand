# SSE 断线重连设计

引入一个以会话为粒度的事件广播器，将事件生产与事件推送解耦。

流式聊天会话在向当前连接推送事件的同时，也会将事件广播到该广播器，从而支持多个订阅者。

新增一个端点，客户端通过该端点订阅指定会话的事件流。广播器内部为每个订阅者维护一个队列，订阅者循环从队列中读取事件并写入 SSE 响应；当收到结束信号时退出循环。

前端封装了可自动重连（指数退避）的 SSE 订阅模块。页面加载时若检测到服务端正在处理中且本地没有活跃流，则初始化流状态后启动订阅。用户发送新消息前会中断旧订阅，避免同一事件被重复处理。原有的直接流式推送保持不变。
